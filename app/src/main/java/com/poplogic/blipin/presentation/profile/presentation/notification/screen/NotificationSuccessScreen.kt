package com.poplogic.blipin.presentation.profile.presentation.notification.screen

import androidx.compose.foundation.layout.Column
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.poplogic.blipin.presentation.profile.presentation.notification.NotificationScreenUiState
import com.poplogic.blipin.presentation.profile.presentation.notification.NotificationScreenViewModel

@Composable
fun NotificationSuccessScreen(
    uiState: NotificationScreenUiState.Success,
    innerPadding: androidx.compose.foundation.layout.PaddingValues,
    viewModel: NotificationScreenViewModel,
    modifier: Modifier = Modifier,
) {
    Column(modifier = modifier) {
    }
}
